[project]
name = "marker-tracking"
version = "0.1.0"
description = "Simple CV marker tracking without ML"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ffmpeg>=1.4",
    "jupyter>=1.1.1",
    "matplotlib>=3.10.7",
    "opencv-python>=4.12.0.88",
    "scikit-learn>=1.7.2",
]
